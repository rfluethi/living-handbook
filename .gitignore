# Dependencies
/vendor/
/node_modules/

# Build artifacts (never commit the built zip or an extracted copy)
/build/
/dist/
/living-handbook/
living-handbook-*.zip
*.zip

# Tooling caches
.phpunit.result.cache
.phpcs.cache

# Build tooling kept locally, not in the repository (see bin/build.sh)
/tools/

# Local WordPress test library
/wordpress/
/tmp/

# Compiled translations
/languages/*.mo

# Leftovers of a patch run (patch -F3 writes these next to the file)
*.orig
*.rej

# Editor and OS
.idea/
.vscode/
.DS_Store
Thumbs.db
*.log
wp-tests-config.php
