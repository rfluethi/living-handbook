# Getting started

This area goes from installing the plugin to your first page. It is a folder with a `README.md`, which is why you see this introductory text here. The pages of the area are in the navigation tree on the left.

An area **without** such a `README.md` gets an automatic list of its pages as cards on import instead. The "Upkeep" area shows that.

## Transport-Metadaten
* Reihenfolge: 2
