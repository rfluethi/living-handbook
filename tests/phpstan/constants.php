<?php
/**
 * Constant stubs for static analysis only.
 *
 * These constants are defined at runtime in living-handbook.php from function
 * calls (plugin_dir_url, plugin_dir_path) that PHPStan cannot evaluate, so it
 * does not register them. They are declared here so analysis knows they exist.
 * This file is never loaded at runtime and is excluded from the plugin zip.
 *
 * @package LivingHandbook
 */

define( 'LIVING_HANDBOOK_DIR', '' );
define( 'LIVING_HANDBOOK_URL', '' );
